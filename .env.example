# Copy this file to .env.local and fill in real values for Firebase mode
# All VITE_ prefixed vars are exposed to the browser via import.meta.env
# ⚠️ NEVER commit .env.local to GitHub — it's in .gitignore

# Set to "true" to use mock data (no Firebase required)
# Set to "false" to use real Firebase
VITE_USE_MOCK=true

# Firebase config for browser (exposed to frontend, safe to commit this template)
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=the-boycott-app.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=the-boycott-app
VITE_FIREBASE_STORAGE_BUCKET=the-boycott-app.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=615139729501
VITE_FIREBASE_APP_ID=1:615139729501:web:f5745adaaef3ea53eb2db4

# Node.js script config (for seed-firestore.mjs only)
# Used when running: node scripts/seed-firestore.mjs
FIREBASE_API_KEY=your_api_key_here

# Public app URL (for social sharing)
VITE_APP_URL=https://boycott.app
